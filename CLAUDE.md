# about-dog — 「犬と暮らすための本」

科学的根拠と実際の飼い主の記録の両方から書かれた、犬と暮らすためのリファレンス。
**これから初めて犬を飼う一人の読者に向けて書いた本**だが、成果物は
**GitHub の public リポジトリとして公開し、GitHub Pages でも配信する**（依頼者の決定・2026-08-27）。
公開が前提なので、特定の個人の私事・第三者の私事・実名を挙げた批判を本文に持ち込まない。

## 作業を始める前に必ず読むもの

1. **`docs/decisions/04-resume-plan.md`** — いまどこまで進んでいて、次に何をするか
2. `docs/decisions/02-style-guide.md` — 執筆ルール。章を書くなら必読
3. `docs/decisions/00-grilling-outcome.md` — 設計方針と依頼者の前提
4. `docs/decisions/05-research-prompts.md` — 残りの調査エージェント用プロンプト

## この本の核になっている原則（崩さないこと）

- **出典が確認できない主張は書かない。** 不明なものは「不明」と書く
- **体験談と科学的根拠を視覚的に分離する。** 体験談は `<Experience>` の中だけ
- **論争があるものは論争があると書く。** `<Disputed>` で日本の慣行と国際指針を並べる
- **リスクを書いたら、その隣に必ず対処法を置く。** 孤立した脅し文句を作らない
- **「重要である」で終わる文を書かない。** 数値・手順・時間・失敗例まで落とす
- **専門用語は消さずに `<Term>` で包む。** 獣医に相談するとき正しい語が武器になるため

## 変更してはいけないもの

- `src/styles/global.css` の `--font-sans` — 依頼者による明示指定
- 絵文字を使わない方針（アイコンは `@lucide/astro`）

## コマンド

```bash
pnpm start       # ビルドして http://localhost:4321 で開く（検索が使えるのはこちら）
pnpm dev         # 開発サーバー。速いが全文検索は動かない
pnpm build       # ビルドのみ
```

## 技術上の注意

- MDX からコンポーネントを使うのに **import は不要**（`src/pages/chapters/[...slug].astro` が注入している）
- Lucide の props は**ケバブケース**（`stroke-width`）。camelCase は無効属性として出力されて効かない
- 付録（用語集・論争一覧・出典一覧）は `src/lib/extract.ts` が**本文から自動生成**する。手で二重管理しない
- 検索インデックス（Pagefind）はビルド時のみ生成される
- **`pnpm dev` は起動時点のコンテンツしか読まない。**章を追加しても目次に出ないので、
  表示を確認するときは必ず `pnpm start`（ビルド＋配信）を使う。
  「目次が消えた」という誤解が実際に起きている
- ビルド検証用の仮ページ `src/content/chapters/_smoke.mdx` は削除済み。復活させない

## 調査エージェントを起動するときの必須指示

WebSearch の予算はセッション全体で共有される。前回18本同時起動して枯渇させた。

- 一度に走らせるのは **4〜6本まで**
- 各エージェントに「**WebSearch は最大15回**。超えたら WebFetch に切り替える」と明示
- URL が既知の一次情報（PubMed / 学会公式 / e-Gov / 環境省）は WebFetch で直接取得させる
- 「調査の途中で一度ファイルに書き出す」と指示する（中断で成果が消えるため）
